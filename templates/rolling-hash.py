'''

Features

-RollingHash(string,base,mod)
    (default mod as well)

-remove from an index
    remove(idx)

-add from an index
    add(idx)

-method that takes in a window of size k, and computes the rolling hash of it 
    having some way to pass a string
    and a window size
    and then getting a list of all the hashed values

-compute all prefix and suffix hashes


-method for retrieving hash of a substring in O(1) time with prefix math
    
'''